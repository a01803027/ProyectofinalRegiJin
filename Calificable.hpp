// Clase hecha por Jin
#ifndef CALIFICABLE_HPP
#define CALIFICABLE_HPP

class Calificable
{
private:

protected:

    //virtual void calificar(int) = 0;
    virtual float promedio() = 0;
    
};

#endif